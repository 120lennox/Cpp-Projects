#include <iostream>
#include <stack>
using namespace std;

template <typename input> // this allows you to write code with any datatype

void undo(input data){
    stack<input> newStack;
    newStack.pop(input);
}

template <typename T>

void red(T data){
    stack<T> newStack;
    newStack.push(T)
}

//test func 
int main(){
    return 0;
}
